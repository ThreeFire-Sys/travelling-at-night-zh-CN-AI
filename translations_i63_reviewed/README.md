# Translation chunk outputs

Each `chunk_NNN.jsonl` file contains one compact object per source entry:

```json
{"id":"TAN-…","translation":"…","status":"translated","notes":""}
```

Source chunks and full context are generated under `build/worklist/chunks/`.
